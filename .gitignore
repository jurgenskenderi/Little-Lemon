node_modules/
dist/
# Build *output*. Note this matches at any depth, so never name a source
# directory `build/` — it will be silently untracked and only fail in CI.
build/
*.log
.DS_Store

# local env + secrets
.env
.env.local

# sqlite databases and scraper cache
*.db
*.db-journal
*.db-wal
*.db-shm
server/data/
server/.cache/

# expo
.expo/
.expo-shared/
web-build/
